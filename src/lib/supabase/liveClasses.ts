import { supabase } from "../supabase"; export async function getLiveClasses() { const { data } = await supabase.from("live_classes").select("*"); return data || []; } export async function createLiveClass(data) { const { data: result } = await supabase.from("live_classes").insert([data]).select().single(); return result; } export async function deleteLiveClass(id) { const { error } = await supabase.from("live_classes").delete().eq("id", id); return !error; }
